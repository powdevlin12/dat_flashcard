<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Toàn bộ cache dir (export + backup files) -->
    <cache-path
        name="cache_files"
        path="." />
    <!-- External files dir nếu cần -->
    <external-files-path
        name="external_files"
        path="." />
</paths>
